NDG Security Beta Development Version
___________________________________________

To install:

1) Get Egg setup tools installed if not already present.  
$ python ez_setup.py

2) Install NDG Security egg
$ easy_install dist/<filename>.egg

Nb. as precursor to the above you may need to set http_proxy if your site uses one:
$ export http_proxy=http://your-site-proxy.somewhere
